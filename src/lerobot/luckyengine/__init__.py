"""LuckyEngine/Hazel gRPC integration (experimental).

This package intentionally generates Python gRPC stubs dynamically from
`LuckyEngine/Hazel-ScriptCore/.../hazel_rpc.proto` (same pattern as `LuckyEngine/scripts/grpc_env.py`)
so we don't commit generated files into the repo.
"""


